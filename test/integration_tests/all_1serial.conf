default_action reject;  # reject unmatched packets

# UDP interface.
udp {
    port 14500;
}

# Serial interface.
serial {
    device ./ttyS0;
    baudrate 57600;
}

# Default chain.
chain default
{
    reject if HEARTBEAT;
    accept;
}
